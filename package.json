{
  "name": "contentin-client",
  "version": "1.0.0",
  "description": "Frontend client for Content.in web application",
  "scripts": {
    "start": "live-server --port=3000",
    "build": "parcel build index.html --dist-dir dist"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "parcel": "^2.9.3"
  }
}
